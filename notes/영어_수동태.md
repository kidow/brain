## 수동태 기본 구조 — be + p.p.

수동태 = `be 동사 + 과거분사(p.p.)`

변환 3단계:
1. 목적어 → 주어로
2. 동사 → `be + p.p.`로
3. 원래 주어 → `by + 명사`로 (생략 가능)

| | 문장 |
|---|---|
| 능동 | The chef **cooked** the meal. |
| 수동 | The meal **was cooked** by the chef. |

`have + p.p.` = 현재완료 능동태. 수동태는 `be`가 핵심, `have` 아님.
